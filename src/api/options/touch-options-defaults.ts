import { TouchInteractionOptions } from '../../model/chart-model';

export const touchOptionsDefaults: TouchInteractionOptions = {
	disableCrosshair: true,
	enlargeHandles: true,
	handleRadius: 11,
};
